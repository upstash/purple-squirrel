export const NEXTUI_COLORS = ["default", "secondary", "warning", "primary", "success", "danger"] as const;

export const CONSOLE_TABS = ["query-terminal", "recent-queries", "saved-queries"] as const;

export const DASHBOARD_TABS = ["positions", "applications", "users"] as const;

export const SETUP_TABS = ["start", "positions", "applications", "complete"] as const;

export const QUERY_TERMINAL_SETTINGS_TABS = ["filters", "ranking"] as const;